Commandes utilisées : `ng new my-app --standalone`, `ng generate component home --standalone`, `ng generate component about --standalone`.

Routes fonctionnelles : `/` → HomeComponent, `/about` → AboutComponent, menu de navigation avec `<a routerLink>` et `<router-outlet>`.
